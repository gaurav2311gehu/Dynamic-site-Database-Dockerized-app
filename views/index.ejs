<!DOCTYPE html>
<html>
<head>
  <title>Task Manager</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    :root {
      --bg: #f5f5f7;
      --text: #1d1d1f;
      --card: rgba(255, 255, 255, 0.6);
    }

    body.dark {
      --bg: #0f0f10;
      --text: #f5f5f7;
      --card: rgba(40, 40, 42, 0.6);
    }

    body {
      margin: 0;
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
      background: var(--bg);
      color: var(--text);
      transition: 0.4s ease;
    }

    header {
      text-align: center;
      padding: 80px 20px 40px;
      background: linear-gradient(120deg, #0071e3, #6e00ff);
      color: white;
    }

    header h1 { font-size: 54px; margin: 0; }

    .container {
      max-width: 750px;
      margin: auto;
      padding: 30px 20px;
    }

    .card {
      backdrop-filter: blur(20px);
      background: var(--card);
      border-radius: 20px;
      padding: 25px;
      margin-bottom: 25px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.1);
    }

    form { display: flex; gap: 10px; }

    input {
      flex: 1;
      padding: 14px;
      border-radius: 12px;
      border: none;
      font-size: 16px;
    }

    button {
      border-radius: 12px;
      border: none;
      padding: 12px 18px;
      cursor: pointer;
      font-weight: 600;
    }

    .add-btn { background: #0071e3; color: white; }
    .delete-btn { background: #ff3b30; color: white; }

    ul { list-style: none; padding: 0; }

    li {
      display: flex;
      justify-content: space-between;
      padding: 14px 0;
      border-bottom: 1px solid rgba(0,0,0,0.08);
    }

    .switch {
      position: relative;
      display: inline-block;
      width: 50px;
      height: 28px;
    }
    .switch input { display: none; }
    .slider {
      position: absolute;
      cursor: pointer;
      background: #ccc;
      border-radius: 34px;
      top: 0; left: 0; right: 0; bottom: 0;
      transition: .4s;
    }
    .slider:before {
      position: absolute;
      content: "";
      height: 22px; width: 22px;
      left: 3px; bottom: 3px;
      background: white;
      border-radius: 50%;
      transition: .4s;
    }
    input:checked + .slider { background: #34c759; }
    input:checked + .slider:before { transform: translateX(22px); }

    footer {
      text-align: center;
      padding: 40px 20px;
      color: #86868b;
    }

    footer a { margin: 0 10px; text-decoration: none; color: inherit; }
  </style>
</head>
<body>

<header>
  <h1>Task Manager</h1>
  <p>Minimal. Powerful. Beautiful.</p>
</header>

<div class="container">

  <div class="card">
    <form action="/add" method="POST">
      <input type="text" name="title" placeholder="What needs to be done?" required />
      <button class="add-btn">Add</button>
    </form>
  </div>

  <div class="card">
    <ul>
      <% tasks.forEach(task => { %>
        <li>
          <span><%= task.title %></span>
          <form action="/delete/<%= task.id %>" method="POST">
            <button class="delete-btn">Delete</button>
          </form>
        </li>
      <% }) %>
    </ul>
  </div>

  <div class="card" style="display:flex; justify-content:space-between; align-items:center;">
    <span>Dark Mode</span>
    <label class="switch">
      <input type="checkbox" id="darkToggle">
      <span class="slider"></span>
    </label>
  </div>

</div>

<footer>
  Built with ❤️  
  <a href="https://linkedin.com/in/YOUR-LINK" target="_blank">LinkedIn</a>
  <a href="https://github.com/YOUR-GITHUB" target="_blank">GitHub</a>
</footer>

<script>
  document.getElementById('darkToggle')
    .addEventListener('change', () => document.body.classList.toggle('dark'));
</script>

</body>
</html>
